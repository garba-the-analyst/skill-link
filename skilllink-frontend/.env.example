# Copy to .env. In local dev this points at your NestJS server; after you
# deploy the backend, point it at that URL instead and rebuild
# (npm run build) — see the root README's "Deploying to Render" section.
VITE_API_URL=http://localhost:3000
